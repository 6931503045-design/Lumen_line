# เหลืออะไรบ้างกว่า JOD tang จะใช้ได้จริง

สถานะ ณ 17 ก.ย. 2026 — ตรวจจากโค้ดจริง ไม่ใช่จากแผนที่วางไว้

---

## ✅ ใช้ได้แล้ววันนี้

วงจรหลักครบแล้ว มีคนใช้ได้จริง:

```
จดเงินผ่านแชท LINE   "กาแฟ 80"                    ✅
อีเมลธนาคารเข้าเอง   ทุก 5 นาที (K PLUS)           ✅
ดูยอด/กราฟบนเว็บ     ล็อกอินด้วย LINE              ✅
งบรายหมวด           + เตือน 80% / 100% ในแชท      ✅
แผนออม              3 ทางเลือก + cold-start        ✅
รายการประจำ          สร้างให้เองทุก 06:00 น.        ✅
เพิ่ม/แก้/ลบ รายการ   ผ่าน API                      ✅
```

---

## 🔴 ต้องทำ — ไม่งั้นมีปัญหาแน่

| # | งาน | ไฟล์ | ถ้าไม่ทำ |
|---|---|---|---|
| 1 | **เปลี่ยนรหัสผ่าน Supabase** | — | รหัสอยู่ใน git history ใครโคลน repo ได้ = เข้า DB ได้ |
| ~~2~~ | ~~**S13 Push**~~ | `line/push.ts`, `services/quota.service.ts` | ✅ เสร็จ 17 ก.ย. |
| ~~3~~ | ~~**job `cleanup`**~~ | `jobs/cleanup.ts` | ✅ เสร็จ 17 ก.ย. |
| ~~4~~ | ~~**job `planCheck`**~~ | `jobs/planCheck.ts` | ✅ เสร็จ 18 ก.ย. |
| ~~5~~ | ~~**โอนเข้าแผน**~~ | `POST /api/plans/:id/transfer` | ✅ เสร็จ 18 ก.ย. |

ข้อ 2-5 เกี่ยวกันหมด ทำติดกันเร็วกว่าแยกทำ — **เสร็จครบแล้ว 18 ก.ย.**
เหลือข้อ 1 ที่ต้องทำเองที่ Supabase

---

## 🟡 ควรมี — ไม่งั้นใช้งานไม่สะดวก

| งาน | ตอนนี้เป็นยังไง |
|---|---|
| **UI ใหม่** (ทีม frontend) | ดู `docs/UI_CONTRACT.md` |
| **คำสั่งแชท** `สรุป` `เหลือ` `งบ` `ออม` `ช่วยเหลือ` | บอทตอบได้แค่จดเงิน พิมพ์คำสั่งอื่นไม่รู้เรื่อง — **P0 ใน SPEC** |
| **S5.2 `safeToSpend`** | "ใช้ได้วันละเท่าไหร่" ขึ้น `—` — **P0 ใน SPEC** |
| **`PATCH /api/categories/:id`** | แก้ชื่อ/อีโมจิ/สลับหมวดจำเป็นไม่ได้ |
| **job `dailySummary`** | ไม่มีสรุปประจำวัน (ต้องมี Push ก่อน) |
| **S5.7 จำลองการซื้อ** | ช่องในหน้าวิเคราะห์ยังคำนวณมั่ว (`value * 0.6`) |
| **S4 keyword/learned** | ใช้ regex อย่างเดียว / ทำแล้วรายการจากอีเมลจะมีหมวด → ปลดล็อกการเตือนงบให้ด้วย |

---

## 🟢 ทำทีหลังได้ — ไม่ใช่ตัวขวาง

| งาน | ทำไมรอได้ |
|---|---|
| **S11 AI** (6 ไฟล์ว่าง) | ⚖️ G4 บอกว่าปิด AI แล้วแอปต้องใช้ได้ — ออกแบบให้เป็นของเสริมตั้งแต่ต้น |
| **S12 pending_actions** | จำเป็นเมื่อมี AI เท่านั้น (G2 คุมเฉพาะ AI) |
| **S9 อ่านสลิป** | ต้องมี AI Vision ก่อน |
| **Rich menu** | ความสะดวก ไม่ใช่ฟังก์ชัน |
| **Rate limit `/api`** | ต้องเพิ่ม dependency — SPEC บอกให้ขออนุมัติก่อน |

---

## 📊 ความคืบหน้าตาม SPEC

| หัวข้อ | สถานะ | หมายเหตุ |
|---|---|---|
| S1 LINE Gateway | ✅ | ขาดรับรูป (`imageHandler` ว่าง) |
| S2 Frontend | ✅ | กำลังเปลี่ยนเป็น UI ใหม่ |
| S3 Core API | ✅ | |
| S4 Text Parser | 🟡 | มีแต่ regex L1 |
| S5 Money Engine | 🟡 | summary·budget·plan·recurring·ติดตามแผน ✅ / ขาด S5.2, S5.7 |
| S6 Database | ✅ | |
| S7 Scheduler | 🟡 | **4 จาก 5 job** — เหลือ dailySummary |
| S8 Email Ingestion | ✅ | ทดสอบกับเงินจริงแล้ว |
| S9 Slip Reader | ❌ | |
| S10 Dedup | ✅ | |
| S11 AI Service | ❌ | 6 ไฟล์ยังว่าง |
| S12 Pending Actions | ❌ | ตาราง `pending_actions` ยังไม่มีโค้ดแตะ |
| S13 Push & Quota | ✅ | |

ตารางที่ยังไม่มีโค้ดแตะเลย: `user_keyword_map`
(`pending_actions` · `push_log` · `ai_usage_log` มีโค้ดแตะแล้วจาก cleanup/push)

---

## 💡 ลำดับที่แนะนำ

```
1. เปลี่ยนรหัส Supabase        ← ไม่ต้องรอใคร ทำได้เลย
2. S13 Push + job cleanup      ← ✅ เสร็จแล้ว
3. job planCheck               ← ✅ เสร็จแล้ว
4. โอนเข้าแผน                  ← ✅ เสร็จแล้ว
5. คำสั่งแชท สรุป/เหลือ/งบ      ← ถัดไป: บอทเริ่มคุยรู้เรื่อง
6. S5.2 safeToSpend
```

ทำครบ 1-6 แล้ว JOD tang จะใช้ได้จริงแบบไม่มีรูโหว่
ส่วน AI กับอ่านสลิปค่อยว่ากันทีหลัง ตามที่ G4 ออกแบบไว้
