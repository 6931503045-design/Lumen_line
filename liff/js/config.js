// ไฟล์นี้ทำหน้าที่อะไร: ค่าตั้งต้นของหน้า LIFF ที่ต้องกรอกเองก่อนใช้งานจริง
// ใครรับผิดชอบ: ④ Frontend
// ⚠️ ไฟล์นี้ถูกส่งไปที่เบราว์เซอร์ของผู้ใช้ ห้ามใส่ความลับใดๆ (token, service key, รหัสผ่าน)
//    LIFF ID กับ URL ของ backend ไม่ใช่ความลับ ใส่ได้ปกติ
//
// กรอก 2 ค่านี้แล้วหน้า LIFF จะดึงข้อมูลจริงของผู้ใช้แทนข้อมูลจำลอง
// ถ้าปล่อยว่างไว้ หน้าเว็บจะทำงานต่อได้ในโหมดตัวอย่าง (ข้อมูลจำลอง) พร้อมขึ้นแถบบอกสถานะ

window.JODTANG_CONFIG = {
  // LIFF ID จาก LINE Developers > LINE Login > LIFF เช่น "2000000000-AbCdEfGh"
  liffId: '',

  // URL ของ backend ที่ deploy แล้ว ไม่ต้องมี / ปิดท้าย เช่น "https://jodtang.onrender.com"
  // ถ้า backend เสิร์ฟหน้า LIFF เองจากโดเมนเดียวกัน ปล่อยว่างไว้ได้
  apiBaseUrl: '',
};
